name: Domain Consistency

on:
  pull_request:
  push:
    branches: [main]

jobs:
  domain-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Domain consistency check
        run: node scripts/check-domains.cjs
